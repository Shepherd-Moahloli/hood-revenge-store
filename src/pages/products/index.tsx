import {useState} from 'react';
import {useRouter} from 'next/router';
import {products} from '../../lib/catalog';
import ProductCard from '../../components/ProductCard';
export default function Shop(){const router=useRouter();const [query,setQuery]=useState(''),[sort,setSort]=useState('featured');const category=typeof router.query.category==='string'?router.query.category:'All';let visible=products.filter(p=>(category==='All'||p.category===category)&&p.title.toLowerCase().includes(query.toLowerCase()));if(sort==='low')visible=[...visible].sort((a,b)=>a.price-b.price);if(sort==='high')visible=[...visible].sort((a,b)=>b.price-a.price);return <main id="main" className="section"><p className="eyebrow">COLLECTION 001 / SAMPLE CATALOGUE</p><h1 className="page-title">YOUR NEXT<br/>EVERYDAY.</h1><div className="shop-tools"><div className="filters" aria-label="Product categories">{['All','Hoodies','Tees','Accessories'].map(c=><button key={c} aria-pressed={category===c} className={category===c?'active':''} onClick={()=>router.push(c==='All'?'/products':`/products?category=${c}`,undefined,{shallow:true})}>{c}</button>)}</div><div className="search-sort"><input aria-label="Search products" placeholder="Search pieces…" value={query} onChange={e=>setQuery(e.target.value)}/><select aria-label="Sort products" value={sort} onChange={e=>setSort(e.target.value)}><option value="featured">Featured</option><option value="low">Price: low to high</option><option value="high">Price: high to low</option></select></div></div><p className="result-count" aria-live="polite">{visible.length} pieces · Sample prices in ZAR</p><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">{visible.map(p=><ProductCard key={p.id} product={p}/>)}</div>{!visible.length&&<div className="empty-state"><h2>No matching pieces.</h2><p>Try another search or category.</p><button className="button" onClick={()=>{setQuery('');router.push('/products')}}>Reset filters</button></div>}</main>}
